# Gradle / Android
.gradle/
build/
**/build/
local.properties
android-sdk/
jdk-17/
*.iml
.idea/
.DS_Store
captures/
.externalNativeBuild/
.cxx/

# Large / local junk
*.hprof

# Keystores (never commit Play Store / private keys)
*.jks
*.keystore
keystore.properties
# Excepție: keystore public pentru sideload GitHub (aceeași semnătură la fiecare release)
!keystore/
!keystore/dlpulse-sideload.jks

# Built APKs (attach to GitHub Releases instead of committing)
*.apk
